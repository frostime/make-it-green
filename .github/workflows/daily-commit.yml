jobs:
  commit:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run daily commit script
        run: |
          chmod +x ./make-commit.sh
          ./make-commit.sh
